/*
function showTodo(todo: {title:string, text:string}){
    console.log(todo.title + " " + todo.text);
}
let myTodo = {title:'Thrash', text: 'Take out thrash'};

showTodo(myTodo)
*/
function showTodo(todo) {
    console.log(todo.title + " " + todo.text);
}
var myTodo = { title: 'Thrash', text: 'Take out thrash' };
showTodo(myTodo);
